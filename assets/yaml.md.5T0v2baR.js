import{_ as t,o as s,c as a,a2 as n}from"./chunks/framework.yse_Y9k2.js";const k=JSON.parse('{"title":"YAML","description":"","frontmatter":{},"headers":[],"relativePath":"yaml.md","filePath":"yaml.md"}'),i={name:"yaml.md"};function o(d,e,l,r,h,p){return s(),a("div",null,[...e[0]||(e[0]=[n(`<h1 id="yaml" tabindex="-1">YAML <a class="header-anchor" href="#yaml" aria-label="Permalink to &quot;YAML&quot;">​</a></h1><h2 id="layout" tabindex="-1">Layout <a class="header-anchor" href="#layout" aria-label="Permalink to &quot;Layout&quot;">​</a></h2><p>One directory, one workspace file, one YAML file per database:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>.</span></span>
<span class="line"><span>├── workspace.yaml</span></span>
<span class="line"><span>└── databases/</span></span>
<span class="line"><span>    ├── projects.yaml</span></span>
<span class="line"><span>    └── tasks.yaml</span></span></code></pre></div><p><code>workspace.yaml</code> holds the global sections — <code>version</code>, <code>workspace</code>, <code>lifecycle</code>. A file in <code>databases/</code> only declares databases: without this rule, any file could divert the write target or erase a declaration, and the last one loaded would win.</p><h2 id="workspace-yaml" tabindex="-1">workspace.yaml <a class="header-anchor" href="#workspace-yaml" aria-label="Permalink to &quot;workspace.yaml&quot;">​</a></h2><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># workspace.yaml</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">version</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">workspace</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  parent_page_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">33333333-3333-4333-8333-333333333333</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">lifecycle</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  prevent_destroy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">database.projects</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Field</th><th>Required</th><th>Role</th></tr></thead><tbody><tr><td><code>version</code></td><td>yes</td><td>format version of the configuration — <code>1</code></td></tr><tr><td><code>workspace.parent_page_id</code></td><td>yes</td><td>the Notion page under which databases are created</td></tr><tr><td><code>lifecycle.prevent_destroy</code></td><td>no</td><td>list of <code>database.&lt;key&gt;</code>: acknowledgement, shown in the plan — blocks nothing</td></tr><tr><td><code>lifecycle.allow_data_loss</code></td><td>no</td><td>list of <code>database.&lt;key&gt;</code>: acknowledgement, shown in the plan — blocks nothing</td></tr></tbody></table><h2 id="databases-yaml" tabindex="-1">databases/*.yaml <a class="header-anchor" href="#databases-yaml" aria-label="Permalink to &quot;databases/*.yaml&quot;">​</a></h2><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># databases/tasks.yaml</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">databases</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">key</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">tasks</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Tasks</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    properties</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      Name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">title</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      Estimate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">number</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        format</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">number</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      Statut</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">status</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        options</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">key</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">todo</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">            name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">À faire</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">            group</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">To-do</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">key</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">done</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">            name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Fait</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">            group</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Complete</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Field</th><th>Required</th><th>Role</th></tr></thead><tbody><tr><td><code>key</code></td><td>no — derived from <code>name</code> when absent</td><td>stable identity: it anchors the database across a rename and is what the state and <code>import</code> use to identify it</td></tr><tr><td><code>name</code></td><td>yes</td><td>display name shown in Notion</td></tr><tr><td><code>description</code></td><td>no</td><td>shown under the database&#39;s name in Notion</td></tr><tr><td><code>icon</code></td><td>no</td><td>emoji shown in Notion</td></tr><tr><td><code>properties</code></td><td>yes</td><td>each property, by name — see <a href="#property-types">Property types</a></td></tr></tbody></table><div class="warning custom-block"><p class="custom-block-title">A key derived from <code>name</code> moves when you rename</p><p>Without an explicit <code>key</code>, notion-seed derives one from <code>name</code>. Rename the database in the YAML without setting <code>key</code> first, and the derived key changes with it: notion-seed cannot tell the result from a new database — the old key comes out orphaned and goes to the trash, the new one is created empty. Set <code>key</code> explicitly before renaming a database you want to keep.</p></div><h2 id="property-types" tabindex="-1">Property types <a class="header-anchor" href="#property-types" aria-label="Permalink to &quot;Property types&quot;">​</a></h2><p>Ten types are managed today:</p><table tabindex="0"><thead><tr><th>Type</th><th>Extra fields</th></tr></thead><tbody><tr><td><code>title</code></td><td>— (exactly one per database)</td></tr><tr><td><code>rich_text</code></td><td>—</td></tr><tr><td><code>number</code></td><td><code>format</code></td></tr><tr><td><code>url</code></td><td>—</td></tr><tr><td><code>select</code></td><td><code>options</code></td></tr><tr><td><code>multi_select</code></td><td><code>options</code></td></tr><tr><td><code>status</code></td><td><code>options</code>, each with a required <code>group</code></td></tr><tr><td><code>date</code></td><td>—</td></tr><tr><td><code>checkbox</code></td><td>—</td></tr><tr><td><code>people</code></td><td>—</td></tr></tbody></table><p>A property of any other type present in Notion is left untouched — see <a href="#what-is-not-declared">What is not declared</a>.</p><h2 id="options" tabindex="-1">Options <a class="header-anchor" href="#options" aria-label="Permalink to &quot;Options&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Field</th><th>Required</th><th>Role</th></tr></thead><tbody><tr><td><code>key</code></td><td>no</td><td>anchors the option&#39;s identity across a rename — see below</td></tr><tr><td><code>name</code></td><td>yes</td><td>display name shown in Notion</td></tr><tr><td><code>color</code></td><td>no</td><td>sets the option&#39;s color on creation, one of <code>default</code>, <code>gray</code>, <code>brown</code>, <code>orange</code>, <code>yellow</code>, <code>green</code>, <code>blue</code>, <code>purple</code>, <code>pink</code>, <code>red</code> — changing it on an existing option is withheld, see <a href="/notion-seed/commands#what-it-withholds">What it withholds</a></td></tr><tr><td><code>group</code></td><td>status only, required there</td><td>one of <code>To-do</code>, <code>In progress</code>, <code>Complete</code> — see below</td></tr></tbody></table><div class="warning custom-block"><p class="custom-block-title">Removing a status option rewrites rows</p><p>The API reassigns the rows to another option, without an error. The plan counts them before you apply.</p></div><p>A <code>status</code> property must declare its <code>options</code>, and <code>group</code> is <strong>required</strong> on each of them. A <code>status</code> created without options gets populated by the API with its own default options, which the plan will not have shown — and removing them later silently reassigns the rows. <code>select</code> and <code>multi_select</code> do not have this constraint: their options can be managed by hand without that risk.</p><p><code>group</code> only accepts <code>To-do</code>, <code>In progress</code> or <code>Complete</code>. notion-seed does not choose a group for you: an option sent without <code>group</code> is put by the API in the first group, without an error — hence a write the plan would not have announced. On the other types, <code>group</code> is rejected, like <code>format</code> outside <code>number</code>.</p><p>The <code>key</code> is what anchors an option&#39;s identity across a rename: without it, an option renamed in the YAML comes out as a removal followed by an addition — <code>destructive</code> or <code>silent rewrite</code> depending on the type and the number of rows affected — since it cannot be followed across the name change.</p><h2 id="icon" tabindex="-1">Icon <a class="header-anchor" href="#icon" aria-label="Permalink to &quot;Icon&quot;">​</a></h2><p>The <code>icon</code> — an emoji — is written on the database, never on its data source: measured, writing on the database updates both, writing on the data source makes them diverge. <code>notion-seed</code> only reads the database&#39;s icon: if the data source&#39;s is changed separately, in Notion, it does not see it.</p><h2 id="lifecycle-acknowledgements" tabindex="-1">lifecycle — acknowledgements <a class="header-anchor" href="#lifecycle-acknowledgements" aria-label="Permalink to &quot;lifecycle — acknowledgements {#lifecycle-acknowledgements}&quot;">​</a></h2><div class="danger custom-block"><p class="custom-block-title">prevent_destroy prevents nothing</p><p>Despite its name, a database listed in <code>prevent_destroy</code> goes to the trash like any other when it leaves the YAML. The key only adds a mention to the plan.</p></div><p><code>prevent_destroy</code> and <code>allow_data_loss</code> <strong>no longer block anything</strong>. Despite its name, <code>prevent_destroy</code> does not prevent destruction: these two keys are only acknowledgements, shown under the resource they name. A database removed from the YAML, declared in <code>prevent_destroy</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ntn 0.22.11 — workspace Example Space (33333333-3333-4333-8333-333333333333)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Plan: 0 to add, 0 to change, 1 to destroy</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  - database.tasks  [destructive]</span></span>
<span class="line"><span>      - database.tasks — present in the state, absent from the configuration  [destructive]</span></span>
<span class="line"><span>          → 3 row(s) go to the trash with it.</span></span>
<span class="line"><span>      → declared in lifecycle.prevent_destroy.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Impact: 1 database(s) in the trash with 3 row(s).</span></span></code></pre></div><p>They say &quot;I know what this resource holds&quot;, and nothing more. <code>apply</code> therefore moves a database declared in <code>prevent_destroy</code> to the trash exactly like any other, showing the mention. It is spelled out because a key named <code>prevent_destroy</code> that one would believe to be blocking would be a trap: you would rely on it, and it would not hold you back.</p><p>What stops a command, from now on, is what you ask for in your workflow: <a href="/notion-seed/commands#in-ci"><code>--fail-on</code></a>. What informs is the measurement. What decides is you.</p><h2 id="what-is-not-declared" tabindex="-1">What is not declared <a class="header-anchor" href="#what-is-not-declared" aria-label="Permalink to &quot;What is not declared&quot;">​</a></h2><div class="warning custom-block"><p class="custom-block-title">Undeclared options are destroyed</p><p>An undeclared property is never touched. An undeclared option is removed as soon as its property is written: the API replaces the whole list.</p></div><p>A <strong>property</strong> present in Notion and absent from the YAML is never touched: it appears under <code>Unmanaged — present in Notion, left untouched</code>. The API updates properties one by one, so not declaring it is enough to leave it alone.</p><p>An <strong>option</strong> present in Notion and absent from the YAML, on the other hand, will be destroyed as soon as its property is written: the API replaces the whole list of options instead of merging it. The plan therefore shows it as a removal, with the number of rows that hold it: <code>destructive</code> for <code>select</code> and <code>multi_select</code>, <code>silent rewrite</code> for <code>status</code> — and <code>safe</code> if that number is zero.</p><p>In other words, &quot;not declared = not touched&quot; is true for properties and false for options. It is exactly the kind of gap this tool exists to make visible.</p><h2 id="json-schema" tabindex="-1">JSON schema <a class="header-anchor" href="#json-schema" aria-label="Permalink to &quot;JSON schema&quot;">​</a></h2><p>The full schema is <a href="https://github.com/tykok/notion-seed/blob/main/schema/notion-seed.schema.json" target="_blank" rel="noreferrer"><code>schema/notion-seed.schema.json</code></a>.</p>`,37)])])}const y=t(i,[["render",o]]);export{k as __pageData,y as default};
